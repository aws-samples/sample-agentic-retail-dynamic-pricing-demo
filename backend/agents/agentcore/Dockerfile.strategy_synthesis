# Dockerfile for Strategy Synthesis Agent
# AgentCore Runtime container — linux/arm64, port 8080
#
# Build (standalone, without base):
#   docker build --platform=linux/arm64 \
#     -f backend/agents/agentcore/Dockerfile.strategy_synthesis \
#     -t strategy-synthesis-agent .
#
# Build (using base image):
#   docker build --platform=linux/arm64 \
#     -f backend/agents/agentcore/Dockerfile.strategy_synthesis \
#     --build-arg BASE_IMAGE=retail-pricing-agent-base \
#     -t strategy-synthesis-agent .

ARG BASE_IMAGE=retail-pricing-agent-base

FROM --platform=linux/arm64 ${BASE_IMAGE}

WORKDIR /app

EXPOSE 8080

CMD ["python", "-m", "uvicorn", "backend.agents.agentcore.strategy_synthesis_runtime:app", "--host", "0.0.0.0", "--port", "8080"]
