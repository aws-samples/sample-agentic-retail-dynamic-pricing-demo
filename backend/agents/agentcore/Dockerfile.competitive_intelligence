# Dockerfile for Competitive Intelligence Agent
# AgentCore Runtime container — linux/arm64, port 8080
#
# Build (standalone, without base):
#   docker build --platform=linux/arm64 \
#     -f backend/agents/agentcore/Dockerfile.competitive_intelligence \
#     -t competitive-intelligence-agent .
#
# Build (using base image):
#   docker build --platform=linux/arm64 \
#     -f backend/agents/agentcore/Dockerfile.competitive_intelligence \
#     --build-arg BASE_IMAGE=retail-pricing-agent-base \
#     -t competitive-intelligence-agent .

ARG BASE_IMAGE=retail-pricing-agent-base

FROM --platform=linux/arm64 ${BASE_IMAGE}

WORKDIR /app

EXPOSE 8080

CMD ["python", "-m", "uvicorn", "backend.agents.agentcore.competitive_intelligence_runtime:app", "--host", "0.0.0.0", "--port", "8080"]
